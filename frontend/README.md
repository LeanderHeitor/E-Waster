# Frontend E-Waster

A documentacao do projeto foi centralizada no `README.md` da raiz do repositorio.

Comandos rapidos:

```bash
npm install
npm run dev
npm run build
```

O frontend roda em `http://localhost:5173` e chama a API em `http://localhost:8081/api/v1`.
