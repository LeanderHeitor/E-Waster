# Copie para .env (local) ou configure no painel da hospedagem (Render/Vercel).
#
# URL do backend publicado, INCLUINDO /api/v1 no final.
# Local (padrao, se nao definir nada): http://localhost:8081/api/v1
# Producao no Render, ex:
VITE_API_URL=https://ewaster-backend.onrender.com/api/v1
